import { AdminRewardsClient } from './client';

export const metadata = {
  title: 'Admin - Rewards Status | Proof of Existence',
  description: 'View TIME26 rewards distribution status',
};

export default function AdminRewardsPage() {
  return <AdminRewardsClient />;
}
